function influencerChangeKiyaku(){
        var object_id = $('.current_user_id').val();
        setTimeout(function() {
                $('.current_user_id').val(object_id);
        },500);
        $('.current_user_id').val(object_id);
}